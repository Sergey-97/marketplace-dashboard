# ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ И БЫСТРЫЕ РЕШЕНИЯ

## Вопрос 1: Где взять SUPABASE_URL?

**Ответ:**
```
1. Перейти на https://supabase.com
2. Войти в ваш аккаунт
3. Выбрать проект
4. Settings (иконка шестеренки внизу слева)
5. API
6. Project URL — это ваш SUPABASE_URL

Выглядит так: https://xyzabc.supabase.co
```

## Вопрос 2: Где взять SUPABASE_SERVICE_KEY?

**Ответ:**
```
На той же странице Settings → API
Под Project URL есть табы: Project API keys
Нажимаете на tab "service_role (private)"
Там копируете значение — это ваш SUPABASE_SERVICE_KEY

⚠️ ВАЖНО: Это секретный ключ! Никому не давайте!
```

## Вопрос 3: Как проверить что переменные добавлены правильно?

**Ответ:**
```powershell
# На Render в Web Service:
# Settings → Environment
# Убедитесь что видите все переменные:

PORT=10000
NODE_ENV=production
SUPABASE_URL=https://...
SUPABASE_SERVICE_KEY=...
CORS_ORIGINS=...
FRONTEND_URL=...
OZON_API_KEY=...
WB_API_KEY=...

# Нажмите Save
# Сервис перезагрузится (может занять 30-60 секунд)
```

## Вопрос 4: Сервис перезагружается каждые 15 минут, что делать?

**Ответ:**
```
Обычно это означает что сервис крашится и перезапускается.

Проверьте логи в Render:
1. Dashboard → Web Service → Logs
2. Ищите сообщение об ошибке в конце логов
3. Типичные ошибки:

   ❌ SUPABASE_SERVICE_KEY is not set
   → Добавьте переменную в Environment

   ❌ Cannot read property 'from' of undefined
   → Supabase не подключился, проверьте SUPABASE_URL

   ❌ listen EACCES: permission denied :::10000
   → Порт занят, не меняйте PORT на Render

4. После исправления нажмите "Manual Deploy" для перезагрузки
```

## Вопрос 5: На фронте видим ошибку "Failed to fetch"

**Ответ:**
```
Это обычно означает CORS ошибку.

Проверьте:
1. Какой URL видится в браузере?
   - Адресная строка должна показывать Vercel URL

2. В DevTools → Console → проверьте полное сообщение об ошибке

3. Типичные CORS ошибки:

   "No 'Access-Control-Allow-Origin' header"
   → CORS_ORIGINS на Render не совпадает с фронтенд-URL

   "Response to preflight request doesn't pass access control"
   → Проверьте что CORS_ORIGINS содержит Vercel URL целиком:
      https://marketplace-dashboard-git-master-sergeys-projects.vercel.app
      (без слеша в конце!)

Решение:
1. Render → Settings → Environment
2. Обновите CORS_ORIGINS на правильный Vercel URL
3. Нажмите Save
4. Подождите 30 секунд
5. Refresh страницу в браузере (Ctrl+F5)
```

## Вопрос 6: Как узнать URL моего сервиса на Render?

**Ответ:**
```
1. Render → Web Service → верхняя часть страницы
2. Там написано что-то вроде:
   "Your service will be available at:"
   https://marketplace-backend-XXXX.onrender.com

3. Скопируйте этот URL

Или еще проще:
4. Посмотрите в логах:
   Logs содержат строку типа: "Server running on http://localhost:10000"
   Реальный URL будет: https://marketplace-backend-XXXX.onrender.com
```

## Вопрос 7: Как узнать URL моего фронтенда на Vercel?

**Ответ:**
```
1. Vercel → Projects → найти проект
2. На главной странице проекта видны Domains:
   Production: https://marketplace-dashboard-...vercel.app

Или:
3. Deployments → нажать на последний deployment
4. Visit будет ваш URL
```

## Вопрос 8: Как перезагрузить сервис на Render?

**Ответ:**
```
Способ 1 (быстро):
1. Render Dashboard → Web Service
2. В правом верхнем углу нажать на три точки (⋯)
3. Выбрать "Manual Deploy"
4. Выбрать ветку (main)
5. Нажать "Deploy"

Способ 2 (через Settings):
1. Settings → Build
2. Нажать кнопку "Trigger deploy"

Способ 3 (если совсем сломалось):
1. Settings → Delete service
2. Создать заново (но теперь это займет больше времени)
```

## Вопрос 9: Как проверить что Worker запущен?

**Ответ:**
```
1. Render → Background Workers → marketplace-worker
2. Нажать на сервис
3. Посмотреть статус вверху:
   - "Active" = работает ✅
   - "Building" = еще собирается ⏳
   - "Failed" = ошибка ❌

4. Нажать на Logs и искать:
   "✅ Worker started and ready to process jobs"

Если Worker падает:
1. Проверьте логи на ошибки
2. Убедитесь что SUPABASE_URL в Environment Variables
3. Нажмите "Manual Deploy" для перезагрузки
```

## Вопрос 10: Синхронизация не запускается, что делать?

**Ответ:**
```
Если нажимаете "🔄 Синхронизировать" но ничего не происходит:

Способ 1: Проверьте что Worker работает
1. Render → Background Workers → Logs
2. Ищите ошибки

Способ 2: Проверьте через curl
curl -X POST "https://marketplace-backend-XXXX.onrender.com/api/sync/trigger" \
  -H "Content-Type: application/json" \
  -d '{"marketplace":"ozon","startDate":"2024-11-01","endDate":"2024-11-30"}'

Должно вывести:
{"jobId":"sync-ozon-2024-11-01-2024-11-30","status":"queued"}

Способ 3: Если нет Worker - синхронизация работать не будет
1. Создайте Background Worker (см. DETAILED_SETUP_GUIDE.md)
2. Добавьте все переменные
3. Дождитесь когда он "Active"
4. Попробуйте снова

Способ 4: Проверьте что синхронизация реализована в коде
backend/src/jobs/sync.worker.enhanced.js должен содержать реальный код для OZON/WB
Сейчас там только заглушка (console.log)!
```

## Вопрос 11: Фронтенд не видит данные, как узнать почему?

**Ответ:**
```
1. В браузере нажмите F12 (DevTools)
2. Перейдите на закладку "Network"
3. Нажмите "Загрузить данные"
4. Смотрите запросы:

   Если видите:
   GET /api/orders?... → 200 OK → данные загружены ✅
   GET /api/orders?... → 403 Forbidden → проблема Supabase
   GET /api/orders?... → CORS error → проблема CORS
   GET /api/orders?... → 404 Not Found → API endpoint не существует

5. Нажмите на запрос → Preview/Response → смотрите что вернулось
```

## Вопрос 12: Как просмотреть логи сервера?

**Ответ:**
```
Web Service логи:
1. Render → Web Service → Logs
2. Внизу экрана видны логи в реальном времени

Worker логи:
1. Render → Background Workers → marketplace-worker → Logs
2. Также в реальном времени

Frontend логи:
1. Vercel → Deployments → нажать на deployment → Logs

Браузер логи:
1. DevTools (F12) → Console
2. Ищите красные сообщения об ошибках
```

## Вопрос 13: Как добавить свой маркетплейс или интеграцию?

**Ответ:**
```
1. Сначала реализуйте синхронизацию OZON/WB (см. вопрос 10)
2. Потом скопируйте блок OZON в sync.worker.enhanced.js
3. Поменяйте:
   - marketplace = "yandex" (или ваш маркетплейс)
   - URL API и параметры запроса на ваш маркетплейс
   - Структуру данных чтобы совпадала с Supabase
4. Добавьте переменную YANDEX_API_KEY в Environment на Render
5. Тестируйте локально
6. Коммитьте и пушьте
```

## Вопрос 14: Как экспортировать данные из Supabase?

**Ответ:**
```
Способ 1: Через CSV в Supabase
1. Supabase → SQL Editor
2. SELECT * FROM sales_fact WHERE order_date > '2024-11-01'
3. Нажать "Download" → CSV

Способ 2: Через API
curl "https://marketplace-backend-XXXX.onrender.com/api/orders?startDate=2024-11-01&endDate=2024-11-30" \
  > orders.json

Способ 3: Добавить экспорт в фронтенд
Добавьте кнопку "📥 Скачать как CSV" на фронтенд
```

## Вопрос 15: Как увеличить размер данных которые загружаются?

**Ответ:**
```
Стандартно API возвращает максимум 1000 записей.

Для увеличения:
1. backend/src/index.js → найдите .limit(1000)
2. Измените на .limit(5000) или больше
3. Коммитьте и пушьте
4. Render автоматически перезагрузится

⚠️ Осторожно: слишком большие выборки могут замедлить систему!
```

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

```powershell
# Проверить здоровье backend
curl "https://marketplace-backend-XXXX.onrender.com/health"

# Загрузить данные через API
curl "https://marketplace-backend-XXXX.onrender.com/api/products"

# Запустить синхронизацию
curl -X POST "https://marketplace-backend-XXXX.onrender.com/api/sync/trigger" `
  -H "Content-Type: application/json" `
  -d '{"marketplace":"ozon","startDate":"2024-11-01","endDate":"2024-11-30"}'

# Проверить статус задачи
curl "https://marketplace-backend-XXXX.onrender.com/api/sync/status/JOB_ID"

# Проверить CORS (замените URL на ваши)
curl -i -X OPTIONS "https://marketplace-backend-XXXX.onrender.com/api/products" `
  -H "Origin: https://YOUR-FRONTEND.vercel.app"
```

---

## 💡 СОВЕТЫ ДЛЯ УСПЕХА

1. **Не спешите** - следуйте инструкциям пошагово
2. **Проверяйте логи** - логи содержат нужную информацию
3. **Используйте curl** - так проще тестировать чем через браузер
4. **Коммитьте часто** - не ждите пока всё сломается
5. **Документируйте** - добавляйте комментарии в код
6. **Тестируйте локально** - перед пушем на production
7. **Backup** - сохраняйте копию Supabase перед большими изменениями
8. **Google друзья** - если ошибка - Google вам поможет

---

## 📱 ЕСЛИ НУЖНА ПОМОЩЬ

1. Проверьте этот документ (скорее всего ответ уже здесь)
2. Посмотрите логи (обычно в логах написано что именно сломалось)
3. Попробуйте Google (скопируйте текст ошибки в Google)
4. Спросите на Stack Overflow если совсем запутались
5. Обратитесь в поддержку Render/Vercel/Supabase если проблема в их сервисах

---

**Успехов с настройкой проекта! 🚀**
